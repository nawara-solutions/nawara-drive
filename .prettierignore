node_modules
package-lock.json
.claude

# git-crypt encrypted — checks out as ciphertext for anyone/anything without
# the key (CI included), so Prettier can't meaningfully format-check it.
# Every .md is encrypted except the root README.md (see .gitattributes).
*.md
!/README.md
