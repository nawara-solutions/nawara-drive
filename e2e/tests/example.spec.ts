import { test } from '@playwright/test';

// Scaffold placeholder — replace with real specs once the first app (e.g. the admin
// dashboard) exists. Skipped so `npm run test:e2e` passes cleanly until then.
test.skip('placeholder: replace once an app is scaffolded', async ({ page }) => {
  await page.goto('/');
});
